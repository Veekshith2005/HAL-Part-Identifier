# ── Core ML / Detection ──────────────────────────────────
ultralytics==8.4.56
torch==2.12.0
torchvision==0.23.0

# ── Web Framework ────────────────────────────────────────
flask==3.1.1

# ── Image Processing ─────────────────────────────────────
opencv-python==4.12.0.88
pillow==11.3.0

# ── Data Handling ────────────────────────────────────────
pandas==2.3.0
numpy==2.2.6
openpyxl==3.1.5

# ── Logging ──────────────────────────────────────────────
loguru

# ── Synthetic Dataset Generation (retrain/ project only) ─
# Install these only in the retrain/ virtual environment,
# not required to run the Flask app itself.
# rembg          <- AI background removal (pip install rembg)
# scikit-image   <- installed as rembg dependency
# numba          <- installed as rembg dependency